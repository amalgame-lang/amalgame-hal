/*
 * facade-stub.h — empty runtime header for the Amalgame.Hal facade.
 *
 * amalgame-hal is pure Amalgame (interfaces only, no @c). This stub
 * exists solely because [stdlib].header is required by amc's
 * PackageRegistry.LoadFrom; the user binary's #include is a no-op.
 */
